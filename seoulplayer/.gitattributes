# ============================================================
#  서울플레이어 - Git 속성 설정 (Unity 협업용)
# ============================================================

# 기본: 텍스트 파일 자동 감지 + 줄바꿈 정규화
* text=auto

# ------------------------------------------------------------
#  Unity 씬/프리팹/에셋 - 충돌 시 Unity YAML 병합 도구 사용
#  (Project Settings > Editor > Asset Serialization = Force Text 필수)
# ------------------------------------------------------------
*.unity      merge=unityyamlmerge eol=lf
*.prefab     merge=unityyamlmerge eol=lf
*.asset      merge=unityyamlmerge eol=lf
*.meta       merge=unityyamlmerge eol=lf
*.controller merge=unityyamlmerge eol=lf
*.anim       merge=unityyamlmerge eol=lf
*.mat        merge=unityyamlmerge eol=lf

# C# 스크립트는 항상 텍스트로
*.cs         text diff=csharp

# ------------------------------------------------------------
#  Git LFS (큰 바이너리 관리) - 활성화됨
#  ※ 팀원 모두 클론 후 'git lfs install'을 한 번 실행해야 함!
#  ※ 안 하면 이미지/모델이 깨진 포인터로 보입니다.
# ------------------------------------------------------------
# 이미지
*.png  filter=lfs diff=lfs merge=lfs -text
*.jpg  filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.psd  filter=lfs diff=lfs merge=lfs -text
*.gif  filter=lfs diff=lfs merge=lfs -text
*.tga  filter=lfs diff=lfs merge=lfs -text
# 사운드
*.wav  filter=lfs diff=lfs merge=lfs -text
*.mp3  filter=lfs diff=lfs merge=lfs -text
*.ogg  filter=lfs diff=lfs merge=lfs -text
# 3D 모델 (VARCO 생성물)
*.fbx  filter=lfs diff=lfs merge=lfs -text
*.glb  filter=lfs diff=lfs merge=lfs -text
*.gltf filter=lfs diff=lfs merge=lfs -text
*.obj  filter=lfs diff=lfs merge=lfs -text
# 폰트
*.ttf  filter=lfs diff=lfs merge=lfs -text
*.otf  filter=lfs diff=lfs merge=lfs -text
